// enum DamageClass { error, status, physical, special }

// enum Generation { error, kanto, johto, hoenn, sinnoh, unova, kalos, alola, galar, paldea }

// enum GrowthRate { error, slow, medium, fast, mediumSlow, slowToFast, fastToSlow }

// enum Target {  }